import { Modal } from './Modal';


export default {
  title: 'Modal',
  component: Modal,
};

export const Default = {
  args: {},
  parameters: {
    nextjs: {
      appDirectory: true,
    },
  },
};