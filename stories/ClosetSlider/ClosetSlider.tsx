import React from "react";

export interface ClosetSliderProps {
}

export const ClosetSlider = (props: ClosetSliderProps) => {
  const {} = props;  
  return (
        <div className="w-64 h-96 flex justify-center items-center">
          ClosetSlider
          </div>
    )
}